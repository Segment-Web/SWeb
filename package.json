{
  "name": "segment",
  "version": "0.0.1-beta",
  "private": true,
  "description": "Segment is an extensible open-source messenger with end-to-end encryption.",
  "type": "module",
  "license": "AGPL-3.0-only",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "node apps/server/src/index.js",
    "dev": "node apps/server/src/index.js",
    "check": "node --check apps/server/src/index.js && node --check apps/server/src/gateway.js && node --check apps/server/src/auth.js && node packages/crypto/selftest.js",
    "test:auth": "node apps/server/src/auth.selftest.js"
  },
  "engines": {
    "node": ">=22.13"
  }
}
