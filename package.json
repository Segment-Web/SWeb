{
  "name": "segment",
  "version": "0.1.1",
  "private": true,
  "description": "Segment — расширяемый мессенджер с E2EE и открытым исходным кодом.",
  "type": "module",
  "license": "AGPL-3.0-only",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "node apps/server/src/index.js",
    "dev": "node apps/server/src/index.js",
    "check": "node --check apps/server/src/index.js && node --check apps/server/src/gateway.js && node packages/crypto/selftest.js"
  },
  "engines": {
    "node": ">=20"
  }
}
